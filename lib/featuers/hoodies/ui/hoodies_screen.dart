import 'package:flutter/material.dart';

class HoodiesScreen extends StatelessWidget {
  const HoodiesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text(' Hoodies Screen  '),
      ),
    );
  }
}
